node_modules/
dist/
*.local
tsconfig.tsbuildinfo

# Everything under public/ is GENERATED by `npm run copy:assets` (from the
# moorhen tgz and @rdkit). Never check it in — it's ~120MB of WASM/monomers.
public/
