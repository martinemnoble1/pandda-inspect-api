.venv/
__pycache__/
*.pyc
db.sqlite3
.DS_Store
.env
# PyInstaller freeze output (the binary is built in CI, never committed).
build/
dist/
*.spec.bak

# Electron shell build output + deps (installers are built in CI).
node_modules/
release/
